apply plugin: "war"

bootWar {
    mainClassName = "com.github.lufeimaggie.Exp3App"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
